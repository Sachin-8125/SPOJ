//  Life, the Universe, and Everything (Interactive)

#include<bits/stdc++.h>

using namespace std;

int main() {
    int n;
    while (cin>>n) {
        cout<<n<<endl;
        if (n==42) exit(0);
    }
    
    return 0;
}
